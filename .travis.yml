addons:
  apt:
    packages:
      - python-pygments

script:
  - binary/hugo --baseURL "http://felipesere.com"  --destination .

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
